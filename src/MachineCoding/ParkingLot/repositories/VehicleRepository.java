package MachineCoding.ParkingLot.repositories;

import MachineCoding.ParkingLot.models.Vehicle;

import java.util.Optional;

public class VehicleRepository {
    public Optional<Vehicle> findVehicleByVehicleNumber(String VehicleNumber) {
        return Optional.empty();
    }

    public Vehicle save(Vehicle vehicle) { //Update and insert
        //save the vehilcle
        return null;
    }
}
